require 'spec_helper'

describe LocomotiveGoogleSearch do
  it 'has a version number' do
    expect(LocomotiveGoogleSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
